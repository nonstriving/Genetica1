#include <stdio.h>
#include <stdlib.h>

bool equiv(int size, char *str1, char *str2)
{
  
}

int main()
{
  int size;
  char *str1;
  char *str2;

  scanf("%d", &size);
  scanf("%s", str1);
  scanf("%s", str2);
